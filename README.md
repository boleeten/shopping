
  # shopping_high

  This is a code bundle for shopping_high. The design source file (Figma) is linked in `figma.project.json` — [shopping_high](https://www.figma.com/design/nqfwgsol/shopping_high) (`fileKey`: `nqfwgsol`, root node `0:1`).

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  